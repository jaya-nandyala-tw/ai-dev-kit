# ── Jira Integration ──
# Required for jira_client/
JIRA_BASE_URL=https://<your-org>.atlassian.net
JIRA_API_TOKEN=your-atlassian-api-token-here
# DEV_EMAIL (defined elsewhere in your .env) is reused as the Jira auth email
